version: '3.8'
services:
  jaeger:
    image: jaegertracing/all-in-one:1.50
    container_name: shared_jaeger
    environment:
    # all-in-one defaults to in-memory storage with UNBOUNDED retention. Without this it grew to
    # 3.1 GB -- the largest consumer on the box, larger than any actual service.
    - MEMORY_MAX_TRACES=20000
    ports:
    - "16686:16686"
    - "4317:4317"
    - "4318:4318"
    # Backstop: if the app-level cap is ever wrong, Docker enforces the ceiling instead of the host
    # running out. Development tooling must not be able to starve the services it observes.
    mem_limit: 512m
    restart: unless-stopped
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: shared_zookeeper
    mem_limit: 320m
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
    - 2181:2181
    restart: unless-stopped
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: shared_kafka
    mem_limit: 1600m
    depends_on:
    - zookeeper
    ports:
    - 9092:9092
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://shared_kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    healthcheck:
      test:
      - CMD
      - kafka-broker-api-versions
      - --bootstrap-server
      - localhost:9092
      interval: 15s
      timeout: 20s
      retries: 15
      start_period: 120s
    restart: unless-stopped

  postgres:
    image: kartoza/postgis:16-3.4
    container_name: shared_postgres
    mem_limit: 768m
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASS: ${POSTGRES_PASS}
      POSTGRES_EXTRA_OPTS: -c max_connections=500
      EXTRA_CONF: "wal_level=logical"
    ports:
    - 5433:5432
    volumes:
    - shared_pgdata:/var/lib/postgresql/data
    - ./init-multiple-dbs.sql:/docker-entrypoint-initdb.d/init-multiple-dbs.sql
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -h localhost -U postgres
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: unless-stopped
  redis:
    image: redis:7.2
    container_name: shared_redis
    mem_limit: 192m
    ports:
    - 6379:6379
    volumes:
    - shared_redis_data:/data
    restart: unless-stopped
    command:
    - redis-server
    - --appendonly
    - 'yes'
    - --save
    - '60'
    - '1'
    - --loglevel
    - warning
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 120s
  config-service:
    image: ${REGISTRY}/food-delivery/config-service:${CONFIG_SERVICE_TAG}
    container_name: config-service
    mem_limit: 512m
    user: root
    ports:
    - 8888:8888
    volumes:
    - ./:/config
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=native
    - SPRING_CLOUD_CONFIG_SERVER_NATIVE_SEARCH_LOCATIONS=file:/config
    - SPRING_CONFIG_IMPORT=optional:file:/config/config-service.yml
    - CONFIG_USER=${CONFIG_USER}
    - CONFIG_PASSWORD=${CONFIG_PASSWORD}
    - EUREKA_CLIENT_ENABLED=false
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider --user=$${CONFIG_USER} --password=$${CONFIG_PASSWORD} http://localhost:8888/actuator/health
        || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: unless-stopped
  eureka-server-1:
    image: ${REGISTRY}/food-delivery/eureka-server-1:${EUREKA_SERVER_1_TAG}
    container_name: eureka-server-1
    mem_limit: 512m
    ports:
    - 8761:8761
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - EUREKA_HOSTNAME=eureka-server-1
    - EUREKA_CLIENT_REGISTER_WITH_EUREKA=true
    - EUREKA_CLIENT_FETCH_REGISTRY=true
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider --user=$${EUREKA_USER:-admin} --password=$${EUREKA_PASSWORD:-admin}
        http://localhost:8761/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: unless-stopped
  eureka-server-2:
    image: ${REGISTRY}/food-delivery/eureka-server-2:${EUREKA_SERVER_2_TAG}
    container_name: eureka-server-2
    mem_limit: 512m
    ports:
    - 8762:8761
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - EUREKA_HOSTNAME=eureka-server-2
    - EUREKA_CLIENT_REGISTER_WITH_EUREKA=true
    - EUREKA_CLIENT_FETCH_REGISTRY=true
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider --user=$${EUREKA_USER:-admin} --password=$${EUREKA_PASSWORD:-admin}
        http://localhost:8761/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: unless-stopped
  customer-service:
    image: ${REGISTRY}/food-delivery/customer-service:${CUSTOMER_SERVICE_TAG}
    container_name: customer-service
    mem_limit: 896m
    ports:
    - 8092:8092
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/customer_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/customer_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8092/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  restaurant-service:
    image: ${REGISTRY}/food-delivery/restaurant-service:${RESTAURANT_SERVICE_TAG}
    container_name: restaurant-service
    mem_limit: 896m
    ports:
    - 8094:8094
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/restaurant_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/restaurant_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - KYB_FSSAI_API_URL=http://api-gateway:8080/mock/fssai
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8094/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  delivery-service:
    image: ${REGISTRY}/food-delivery/delivery-service:${DELIVERY_SERVICE_TAG}
    container_name: delivery-service
    mem_limit: 896m
    ports:
    - 8095:8095
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/delivery_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/delivery_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8095/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  payment-gateway:
    image: ${REGISTRY}/food-delivery/payment-gateway:${PAYMENT_GATEWAY_TAG}
    container_name: payment-gateway
    mem_limit: 1024m
    ports:
    - 8085:8085
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - PORT=8085
    - DB_URL=jdbc:postgresql://postgres:5432/payment_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/payment_db
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8085/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  maps-integration:
    image: ${REGISTRY}/food-delivery/maps-integration:${MAPS_INTEGRATION_TAG}
    container_name: maps-integration
    mem_limit: 896m
    ports:
    - 8083:8083
    environment:
    - OLA_MAPS_API_KEY=${OLA_MAPS_API_KEY:-}
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    - SPRING_AUTOCONFIGURE_EXCLUDE=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8083/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  communication-integration:
    image: ${REGISTRY}/food-delivery/communication-integration:${COMMUNICATION_INTEGRATION_TAG}
    container_name: communication-integration
    mem_limit: 1024m
    ports:
    - 8081:8081
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_HOST=postgres
    - DB_PORT=5432
    - DB_NAME=notification_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/notification_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BROKERS=kafka:29092
    - REDIS_HOST=redis
    - REDIS_PORT=6379
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8081/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  api-gateway:
    image: ${REGISTRY}/food-delivery/api-gateway:${API_GATEWAY_TAG}
    container_name: api-gateway
    mem_limit: 896m
    user: root
    ports:
    - 8080:8080
    volumes:
    - ./certs:/certs
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - OLA_MAPS_API_KEY=${OLA_MAPS_API_KEY}
    - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
    - ALLOWED_ORIGIN_PATTERNS=${ALLOWED_ORIGIN_PATTERNS}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - JWT_PUBLIC_KEY_PATH=file:/certs/public.pem
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - REDIS_HOST=redis
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8080/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  identity-service:
    image: ${REGISTRY}/food-delivery/identity-service:${IDENTITY_SERVICE_TAG}
    container_name: identity-service
    mem_limit: 896m
    user: root
    ports:
    - 8086:8086
    volumes:
    - ./certs:/certs
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - JWT_PRIVATE_KEY_PATH=file:/certs/private.pem
    - DB_URL=jdbc:postgresql://postgres:5432/identity_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/identity_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8086/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  government-id-service:
    image: ${REGISTRY}/food-delivery/government-id-service:${GOVERNMENT_ID_SERVICE_TAG}
    container_name: government-id-service
    mem_limit: 1024m
    ports:
    - 8087:8087
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/government_id_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/government_id_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_CLIENT_REGISTER_WITH_EUREKA=true
    - EUREKA_CLIENT_FETCH_REGISTRY=true
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8087/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  food-delivery-app-ui:
    image: ${REGISTRY}/food-delivery/food-delivery-app-ui:${FOOD_DELIVERY_APP_UI_TAG}
    container_name: food-delivery-app-ui
    mem_limit: 128m
    ports:
    - 80:80
    depends_on:
    - api-gateway
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://127.0.0.1/ || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: on-failure
  ledger-service:
    image: ${REGISTRY}/food-delivery/ledger-service:${LEDGER_SERVICE_TAG}
    container_name: ledger-service
    mem_limit: 896m
    ports:
    - 8089:8089
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/ledger_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ledger_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8089/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  chat-service:
    image: ${REGISTRY}/food-delivery/chat-service:${CHAT_SERVICE_TAG}
    container_name: chat-service
    mem_limit: 896m
    ports:
    - 8096:8096
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/chat_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/chat_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - R2_BUCKET_NAME=${R2_BUCKET_NAME}
    - R2_ENDPOINT=${R2_ENDPOINT}
    - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
    - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
    - R2_PUBLIC_URL=${R2_PUBLIC_URL}
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8096/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
#  ondc-integration-service:
#    build:
#      context: ../
#      dockerfile: ONDCIntegrationService/Dockerfile
#    container_name: ondc-integration-service
#    ports:
#    - 8098:8098
#    environment:
#    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
#    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
#    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
#    - EUREKA_USER=${EUREKA_USER:-admin}
#    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
#    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
#    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
#    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
#    - DB_URL=jdbc:postgresql://postgres:5432/ondc_db
#    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
#    - REDIS_HOST=redis
#    - SPRING_DATA_REDIS_HOST=redis
#    depends_on:
#      config-service:
#        condition: service_healthy
#      eureka-server-1:
#        condition: service_healthy
#      postgres:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#    healthcheck:
#      test:
#      - CMD-SHELL
#      - wget --spider -q http://localhost:8098/ondc/actuator/health || exit 1
#      interval: 10s
#      timeout: 5s
#      retries: 10
#      start_period: 120s
#    restart: on-failure
  wallet-service:
    image: ${REGISTRY}/food-delivery/wallet-service:${WALLET_SERVICE_TAG}
    container_name: wallet-service
    mem_limit: 896m
    ports:
    - 8120:8120
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_HOST=postgres
    - DB_PORT=5432
    - DB_NAME=wallet_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/wallet_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - PLATFORM_DEFAULT_CURRENCY=INR
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - KAFKA_BROKERS=kafka:29092
    depends_on:
      config-service:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8120/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  clickhouse:
    image: clickhouse/clickhouse-server:latest
    container_name: shared_clickhouse
    mem_limit: 1024m
    ports:
    - 8123:8123
    - 9000:9000
    volumes:
    - shared_clickhouse_data:/var/lib/clickhouse
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - wget
      - --spider
      - -q
      - http://localhost:8123/ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
  campaign-service:
    image: ${REGISTRY}/food-delivery/campaign-service:${CAMPAIGN_SERVICE_TAG}
    container_name: campaign-service
    mem_limit: 896m
    ports:
    - 8088:8088
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - DB_URL=jdbc:postgresql://postgres:5432/campaign_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/campaign_db
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8088/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  bidding-engine:
    image: ${REGISTRY}/food-delivery/bidding-engine:${BIDDING_ENGINE_TAG}
    container_name: bidding-engine
    mem_limit: 896m
    ports:
    - 8102:8102
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - REDIS_HOST=redis
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - SPRING_AUTOCONFIGURE_EXCLUDE=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8102/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  budget-pacing-service:
    image: ${REGISTRY}/food-delivery/budget-pacing-service:${BUDGET_PACING_SERVICE_TAG}
    container_name: budget-pacing-service
    mem_limit: 896m
    ports:
    - 8103:8103
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - DB_URL=jdbc:postgresql://postgres:5432/budget_db
    - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/budget_db
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8103/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  event-tracking-service:
    image: ${REGISTRY}/food-delivery/event-tracking-service:${EVENT_TRACKING_SERVICE_TAG}
    container_name: event-tracking-service
    mem_limit: 896m
    ports:
    - 8104:8104
    environment:
    - OTLP_ENDPOINT=http://jaeger:4318/v1/traces
    - IDENTITY_HMAC_SECRET=${IDENTITY_HMAC_SECRET}
    - AUCTION_TOKEN_SECRET=${AUCTION_TOKEN_SECRET}
    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
    - EUREKA_USER=${EUREKA_USER:-admin}
    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
    - CLICKHOUSE_HOST=clickhouse
    - CLICKHOUSE_USER=default
    - CLICKHOUSE_PASSWORD=
    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
    - REDIS_HOST=redis
    - SPRING_AUTOCONFIGURE_EXCLUDE=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
    - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      config-service:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      kafka:
        condition: service_healthy
      eureka-server-1:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget --spider -q http://localhost:8104/actuator/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 120s
    restart: on-failure
  api-docs:
    image: nginx:alpine
    container_name: api-docs
    mem_limit: 64m
    ports:
      - "8090:80"
    volumes:
      - ../public-docs:/usr/share/nginx/html:ro
    restart: unless-stopped
#  reviews-service:
#    build:
#      context: ../
#      dockerfile: ReviewsService/Dockerfile
#    container_name: reviews-service
#    ports:
#    - 8099:8099
#    environment:
#    - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
#    - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASS}
#    - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-1:8761/eureka/,http://${EUREKA_USER:-admin}:${EUREKA_PASSWORD:-admin}@eureka-server-2:8761/eureka/
#    - EUREKA_USER=${EUREKA_USER:-admin}
#    - EUREKA_PASSWORD=${EUREKA_PASSWORD:-admin}
#    - PLATFORM_BUSINESS_ZONE=${PLATFORM_BUSINESS_ZONE:-Asia/Kolkata}
#    - PLATFORM_DEFAULT_CURRENCY=${PLATFORM_DEFAULT_CURRENCY:-INR}
#    - SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
#    - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-dev}
#    - CONFIG_SERVER_URL=http://${CONFIG_USER}:${CONFIG_PASSWORD}@config-service:8888
#    - DB_URL=jdbc:postgresql://postgres:5432/reviews_db
#    - KAFKA_BOOTSTRAP_SERVERS=kafka:29092
#    - REDIS_HOST=redis
#    - SPRING_DATA_REDIS_HOST=redis
#    depends_on:
#      config-service:
#        condition: service_healthy
#      postgres:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#    healthcheck:
#      test:
#      - CMD-SHELL
#      - wget --spider -q http://localhost:8085/actuator/health || exit 1
#      interval: 10s
#      timeout: 5s
#      retries: 10
#      start_period: 120s
#    restart: on-failure
volumes:
  shared_pgdata: null
  shared_redis_data: null
  shared_clickhouse_data: null


